const add = (x,y) =>{
    const add = x+y;
    const diff = x-y;
    const mult = x*y;
    const div = x/y;
     const result = add*diff;
     return result;
}
const empty = () => 6;

const output = empty();
console.log(output);